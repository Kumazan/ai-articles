# 2026-02-19

- Installed Claude Code on VPS (`claude` 2.1.47).
- Cloned `Kumazan/polymarket-bond-scanner`, set up venv, ran tests: 291 passed.
- Installed project-scoped Claude plugins for repo (github, context7, code-review, security-guidance, commit-commands, feature-dev, serena).
- Deployed scanner service to systemd (`polymarket-scanner`) on `127.0.0.1:5001`.
- Set up dedicated Cloudflare Tunnel + systemd service (`cloudflared-scanner`) for `scanner.kumax.dev`.
- Added extra trade API protection: `/api/trade` and `/api/trade/<id>` now require `X-Trade-Token` when `TRADE_API_TOKEN` is set; token stored at `/root/.openclaw/workspace/.scanner_trade_token` and loaded via `/etc/polymarket-scanner.env`.
- Kuma confirmed Cloudflare Access app is configured:
  - Apps URL: https://one.dash.cloudflare.com/f6628b05c86b58bf21991282f2950950/access-controls/apps
  - App name: Polymarket Bond Scanner
  - Domain: scanner.kumax.dev
  - App ID: a3044a2d-2ef8-4790-98fb-7c0a7123c024
- Kuma preference/workflow for PR review follow-ups: **patch → push → reply → resolve**.
- Kuma 偏好中大型需求流程：subagent 先用 First Principles + Plan Mode 出計畫，我先審核回報，Kuma 確認後再實作，最後開 PR。
- #49 實作方向最終定稿：不保留 legacy allocator/dual-mode，採單一路徑 fixed-position 評估（預設 position_size=1000、min_profit_pct=0.01、top_n=10、require_full_fill=true）。
- Kuma 特別重視 Profit 與 Polymarket UI 對齊；已修正為按「每筆 allocated amount」逐檔吃單計算 shares/profit，而非用整段深度均價套用。
- PR #50 經多輪 Copilot review 後完成 patch→push→reply→resolve 流程；目前 merge state clean、unresolved threads=0。
- 針對 2026-02-19 晚間新增 review，已再補一輪修正（commit `6c355ff`）：
  - 修正 engine/allocator double simulation（在 BondOpportunity 快取 fixed-position fill 結果，allocator 重用，降低重算並避免 partial-fill 口徑漂移）。
  - 修復 `tools/compare_scoring.py` 的 `dataclasses.replace()` 導致 legacy attrs 掉失問題。
  - 修復 `tools/compare_cap.py` 語意失效，改為工具內自帶 legacy allocator 比較邏輯。
  - CLI 參數驗證補齊（`--position-size` / `--min-profit-pct` / `--top-n`）。
  - 全測試仍通過：`282 passed`。
