# 搜尋 & 篩選

## 功能描述
在 Header 下方加搜尋列，支援全文搜尋標題/描述、優先級多選篩選、標籤多選篩選。篩選結果即時更新，空結果顯示提示。

## 設計決策
- 搜尋狀態放在 KanbanBoard 層級，透過 props 傳遞給 Column/Card
- 不符合篩選條件的卡片直接隱藏（不是 dim）
- 標籤自動從所有卡片收集
- 使用 Chips toggle UI，行動優先設計
